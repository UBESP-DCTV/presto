% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fetch_analytics.R
\name{fetch_analytics}
\alias{fetch_analytics}
\title{Fetch analytics}
\usage{
fetch_analytics(
  metrics = c("users", "newUsers"),
  dimensions = c("userType", "sessionCount"),
  email = NULL,
  view_id = NULL,
  date_range = c("2020-01-01", "yesterday"),
  all_results = FALSE,
  max = ifelse(all_results, -1, 10)
)
}
\arguments{
\item{metrics}{(chr) metrics to consider (see details)}

\item{dimensions}{(chr) dimensions to consider (see details)}

\item{email}{(chr, NULL) email linked to the account investigated,
if NULL it will uses dg.}

\item{view_id}{(chr, NULL) project id to investigate, if NULL the
#PRESTOinsieme will be used.}

\item{date_range}{(chr) a character vector of length two with the
date range (first, last) of interest}

\item{all_results}{(lgl, FALSE) do you want to fetch all the results?}

\item{max}{(int) how many results do you want to fetch? Default is
10 if \code{all_results} is \code{FALSE}, and -1 (ie "all) if \code{all_results}
is \code{TRUE}.}
}
\value{
a \link[tibble:tibble-package]{tibble}
}
\description{
Fetch analytics
}
\details{
The first time a query is made for an account the system
will ask to perform an OAuth authentication to the Google analytics
account, and it will cache the token system wise (ie, accessible
to all the user of the package
\link{googleAnalyticsR}).

To auto-authenticate on package load, add an environment argument
GARGLE_EMAIL via an .Renviron file or otherwise.

Eg in \code{.Renviron} set:

\code{GARGLE_EMAIL=your@email.com}

Metrics ad dimensions can be explored
\href{https://ga-dev-tools.appspot.com/dimensions-metrics-explorer/}{here}
}
\examples{
\dontrun{
  fetch_analytics()
}
}
\seealso{
\href{http://code.markedmondson.me/googleAnalyticsR/articles/setup.html}{googleAnalyticsR's setup}
}
